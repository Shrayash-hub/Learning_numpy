python_list = [1,2,3,4,5]
print(python_list)

import numpy as np
np_ary = np.array([10,20,30])
print(np_ary)

# what is the basic difference between python list and numpy arrays
# 1. we can save different types of elements in a single list but in np arrays we can only save similar types of elements
# 2. np arrays(vectorization) are FASTER than python list
# 3. np arrays takes less amount of MEMORY